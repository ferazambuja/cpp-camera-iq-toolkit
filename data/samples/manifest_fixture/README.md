# Manifest Fixture

Tiny text fixtures with RAW-like filenames for testing public manifest and
filename-series behavior. These are not real RAW captures and should be used
with `--no-exif`.

```bash
./build/camera_iq manifest data/samples/manifest_fixture --no-exif
```
